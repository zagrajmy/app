import { SystemStyleObject } from "@styled-system/css";

const buttonReset: SystemStyleObject = {
  font: "inherit",
  background: "none",
  border: "none"
};

export const styles = {
  buttonReset
};
